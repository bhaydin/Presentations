# Scout AgentOps demo -- deliberately tiny.
# Nothing here needs network access at runtime.
duckdb>=1.0
PyYAML>=6.0

# Optional Azure OpenAI support belongs in the separate MAF environment; see
# requirements-maf.txt and the "second planner" section of README.md.
# openai>=1.40
